snapshots:
  - name: dimlisting
    relation: ref('listing')
    config:
      schema: gold
      database: airbnb
      unique_key: listing_id
      strategy: timestamp
      updated_at: listing_created
      dbt_valid_to_current: "to_date('9999-12-31')"